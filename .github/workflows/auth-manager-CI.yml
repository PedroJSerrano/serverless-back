name: CI de auth-manager
on:
  push:
    branches:
      - develop
      - master
      - 'feature/**'
    paths:
      - 'auth-manager/**'
      - 'pom.xml'

jobs:
  call-ci-workflow:
    uses: ./.github/workflows/reusable-ci.yml
    with:
      module_name: 'auth-manager'