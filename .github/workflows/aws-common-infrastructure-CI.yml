name: CI de AWS Common Infrastructure
on:
  push:
    branches:
      - 'feature/**'
    paths:
      - 'aws-common-infrastructure/**'
      - 'pom.xml'
  pull_request:
    branches:
      - develop
      - master
    paths:
      - 'aws-common-infrastructure/**'
      - 'pom.xml'
  merge_group:
    branches:
      - develop
      - master
    paths:
      - 'aws-common-infrastructure/**'
      - 'pom.xml'

  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write
  statuses: write

jobs:
  call-ci-workflow:
    uses: ./.github/workflows/reusable-ci.yml
    with:
      module_name: 'aws-common-infrastructure'